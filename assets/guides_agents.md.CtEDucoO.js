import{_ as s,o as e,c as d,a2 as a}from"./chunks/framework.D14Z5eOz.js";const p=JSON.parse('{"title":"AI agents (init + hosts)","description":"","frontmatter":{},"headers":[],"relativePath":"guides/agents.md","filePath":"guides/agents.md","lastUpdated":1784865624000}'),o={name:"guides/agents.md"};function i(n,t,r,l,h,c){return e(),d("div",null,[...t[0]||(t[0]=[a(`<h1 id="ai-agents-init-hosts" tabindex="-1">AI agents (init + hosts) <a class="header-anchor" href="#ai-agents-init-hosts" aria-label="Permalink to &quot;AI agents (init + hosts)&quot;">​</a></h1><blockquote><p>Install <strong>one</strong> AI coding agent with <code>sdd init</code> and run the everyday agent loop.</p></blockquote><p>People often confuse IDEs with AI agents, or install every host at once. <code>sdd</code> follows a Spec Kit–style single host: <code>--ai grok | copilot | claude | ollama</code> — clean stubs, one protocol, no extension required.</p><p>Agent files, <code>.sdd/protocol.md</code>, and live <code>active-context.md</code> are set up once at init. Switch later with <code>sdd agents install</code>.</p><p><strong>No skills packs.</strong> One playbook, thin role agents, live context.</p><p><strong>IDEs ≠ AI agents.</strong> VS Code, Cursor, and IntelliJ are editors. <code>sdd init</code> asks which <strong>AI coding agent</strong> to install files for, not which IDE you use.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Path</th><th>Size</th></tr></thead><tbody><tr><td><strong>Protocol</strong> (only full playbook)</td><td><code>.sdd/protocol.md</code></td><td>~1 page</td></tr><tr><td><strong>Live task</strong></td><td><code>.sdd/active-context.md</code></td><td>generated</td></tr><tr><td><strong>Claude Code agents</strong></td><td><code>.claude/agents/*.md</code></td><td>~10 lines each</td></tr><tr><td><strong>GitHub Copilot agents</strong></td><td><code>.github/agents/*.agent.md</code></td><td><strong>same text</strong> as Claude agents</td></tr><tr><td><strong>Grok Build</strong></td><td><code>.grok/rules/sdd.md</code> + <code>AGENTS.md</code></td><td>router rule + pointer</td></tr><tr><td><strong>Ollama</strong></td><td><code>.ollama/sdd.md</code> + <code>AGENTS.md</code></td><td>local models via CLI</td></tr><tr><td><strong>Optional pointer</strong></td><td><code>AGENTS.md</code></td><td>tiny index (all hosts)</td></tr></tbody></table><hr><h2 id="ai-agents-vs-ides" tabindex="-1">AI agents vs IDEs <a class="header-anchor" href="#ai-agents-vs-ides" aria-label="Permalink to &quot;AI agents vs IDEs&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Kind</th><th>Examples</th><th>What SDD installs</th></tr></thead><tbody><tr><td><strong>AI coding agent</strong></td><td>GitHub Copilot, Claude Code, Grok Build, <strong>Ollama</strong></td><td>Thin stubs/rules + protocol</td></tr><tr><td><strong>IDE</strong></td><td>VS Code, Cursor, IntelliJ, etc.</td><td>Nothing — edit files + run <code>sdd</code> in a terminal (no extension required)</td></tr></tbody></table><table tabindex="0"><thead><tr><th>Surface</th><th>Integration</th></tr></thead><tbody><tr><td><strong>CLI</strong></td><td><code>sdd</code> (process owner; Spec Kit–style)</td></tr><tr><td><strong>GitHub Copilot</strong> (AI agent)</td><td><code>.github/agents/*.agent.md</code></td></tr><tr><td><strong>Claude Code</strong> (AI agent)</td><td><code>.claude/agents/*.md</code></td></tr><tr><td><strong>Grok Build</strong> (AI agent)</td><td><code>.grok/rules/sdd.md</code> + <code>AGENTS.md</code> (Grok auto-loads both)</td></tr><tr><td><strong>Ollama</strong> (local AI)</td><td><code>.ollama/sdd.md</code> + <code>AGENTS.md</code>; launch via <code>ollama run</code></td></tr></tbody></table><hr><h2 id="roles" tabindex="-1">Roles <a class="header-anchor" href="#roles" aria-label="Permalink to &quot;Roles&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Agent id</th><th>Role</th><th>Hosts</th></tr></thead><tbody><tr><td><code>sdd</code></td><td>Router — plan or implement from current stage</td><td>Copilot, Claude, Grok, Ollama</td></tr><tr><td><code>sdd-planner</code></td><td>Specs / design / tasks only</td><td>Copilot, Claude</td></tr><tr><td><code>sdd-implementer</code></td><td>Code for active change</td><td>Copilot, Claude</td></tr><tr><td><code>sdd-reviewer</code></td><td>Check against acceptance before verify</td><td>Copilot, Claude</td></tr></tbody></table><p>Bodies are <strong>stubs</strong>: role + “read <code>active-context.md</code> then <code>protocol.md</code>”.<br> All real rules live in <strong><code>.sdd/protocol.md</code> once</strong>.</p><p><strong>Grok / Ollama note:</strong> These hosts use a <strong>single</strong> router brief (not four role files). Pick stage behavior from <code>active-context.md</code>. For Ollama set <code>SDD_OLLAMA_MODEL</code> (default <code>llama3.2</code>) and <code>ollama pull</code> that model first.</p><hr><h2 id="setup-is-one-command" tabindex="-1">Setup is one command <a class="header-anchor" href="#setup-is-one-command" aria-label="Permalink to &quot;Setup is one command&quot;">​</a></h2><p><strong>You only need <code>sdd init</code>.</strong> It installs SDD <strong>and</strong> the chosen AI agent.<br><code>sdd agents install</code> is only for <strong>later</strong> (switch host or reinstall).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># First-time (pick ONE AI — not all hosts)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --here</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> grok</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # only .grok/rules + shared SDD dirs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --here</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> copilot</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # only .github/agents + shared SDD dirs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --here</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> claude</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # only .claude/agents + shared SDD dirs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --here</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ollama</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # only .ollama/sdd.md + shared SDD dirs (local models)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Interactive pick (still one agent)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --here</span></span></code></pre></div><h3 id="what-gets-created" tabindex="-1">What gets created <a class="header-anchor" href="#what-gets-created" aria-label="Permalink to &quot;What gets created&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Always (shared SDD)</th><th>Only for selected <code>--ai</code></th></tr></thead><tbody><tr><td><code>.sdd/</code>, <code>memory/</code>, <code>changes/</code>, <code>domains/</code></td><td><strong>grok</strong> → <code>.grok/rules/sdd.md</code></td></tr><tr><td><code>AGENTS.md</code> when an agent is installed</td><td><strong>copilot</strong> → <code>.github/agents/*.agent.md</code></td></tr><tr><td></td><td><strong>claude</strong> → <code>.claude/agents/*.md</code></td></tr><tr><td></td><td><strong>ollama</strong> → <code>.ollama/sdd.md</code></td></tr></tbody></table><p>Installing one host <strong>removes</strong> other hosts’ agent directories.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Later only</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> agents</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> copilot</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --force</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # switch away from grok</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> agents</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> refresh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                        # update active-context.md after stages</span></span></code></pre></div><hr><h2 id="engineer-workflow-agent-after-process-commands" tabindex="-1">Engineer workflow (agent after process commands) <a class="header-anchor" href="#engineer-workflow-agent-after-process-commands" aria-label="Permalink to &quot;Engineer workflow (agent after process commands)&quot;">​</a></h2><p><strong>Model:</strong> SDD owns process state; the <strong>AI coding agent from init</strong> does the work.</p><table tabindex="0"><thead><tr><th>After this command…</th><th>Agent behavior</th></tr></thead><tbody><tr><td><code>sdd new</code> / <code>next</code> / <code>skip</code> / <code>use</code> / <code>gate</code> / <code>verify</code> / <code>complete</code> / <code>checkout</code> / <code>agent</code> / <code>agents refresh</code> / <code>refine</code></td><td>Writes <code>.sdd/handoff.md</code> + refreshes active-context, then <strong>launches</strong> agent</td></tr><tr><td><code>sdd status</code> / <code>doctor</code> / <code>context</code> / <code>workflows</code> / <code>init</code></td><td><strong>No</strong> agent launch</td></tr></tbody></table><table tabindex="0"><thead><tr><th>Configured agent</th><th>How it runs</th></tr></thead><tbody><tr><td><strong>grok</strong></td><td>CLI spawns <code>grok -p &quot;…&quot;</code> (or interactive <code>grok</code>)</td></tr><tr><td><strong>claude</strong></td><td>CLI spawns <code>claude -p &quot;…&quot;</code></td></tr><tr><td><strong>ollama</strong></td><td>CLI spawns <code>ollama run $SDD_OLLAMA_MODEL &quot;…&quot;</code> (default model <code>llama3.2</code>)</td></tr><tr><td><strong>copilot</strong></td><td>Host UI: open Copilot Chat → agent <code>sdd</code> (handoff refreshed at <code>.sdd/handoff.md</code>)</td></tr></tbody></table><p>Skip launch for one command: <code>--no-agent</code> or <code>SDD_NO_AGENT=1</code>.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --here</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> grok</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> new</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Add feature&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> next</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sdd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> verify</span></span></code></pre></div><p>Stage polish: <a href="./refine">Refine a stage</a>. Full host list: <a href="./../reference/agents">Available agents</a>.</p>`,32)])])}const k=s(o,[["render",i]]);export{p as __pageData,k as default};
