# Deprecated: use release.yml (Release CD) instead.
# Kept as a thin redirect so old bookmarks still do something useful.
name: Publish npm packages (legacy)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "npm dist-tag"
        required: false
        default: "latest"

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Use Release workflow
        run: |
          echo "::notice::This workflow is legacy. Prefer 'Release (CD)' (release.yml)."
          echo "Dispatching is not chained automatically — open Actions → Release (CD)."
          echo "Requested tag: ${{ inputs.tag }}"
